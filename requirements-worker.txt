# Everything a render worker needs, and nothing else.
#
# The bot's own requirements.txt installs aiogram, SQLAlchemy, Pillow,
# fontTools, cryptography and a pp calculator. A worker touches none of them:
# its whole job is to poll an endpoint, run a native binary and post an .mp4
# back. It ran on the full list only because `services/__init__.py` imported
# the card renderer, which dragged in the database behind it — see the lazy
# re-exports there and in `utils/osu/__init__.py`.
#
# Versions are pinned to the same lines as requirements.txt, and a test holds
# them to it: two files that drift are worse than one file that is too big.
#
#     ./venv/bin/pip install -r requirements-worker.txt
#
aiohttp==3.14.1
requests>=2.33.0
python-dotenv==1.2.2

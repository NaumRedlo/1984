aiogram==3.29.1
# 3.14.1+: aiohttp 3.13.3 carried 21 published CVEs (fixed across
# 3.13.4/3.14.0/3.14.1) — and the render worker runs a publicly listening
# aiohttp server, so the server-side fixes matter here, not just client-side.
# aiogram 3.25 capped aiohttp at <3.14, hence the paired aiogram upgrade.
aiohttp==3.14.1
# Beatmap-mirror/replay downloads in utils/osu/danser_renderer.py use requests
# (blocking, run via asyncio.to_thread) instead of aiohttp/httpx: the render
# worker's outbound internet requires an HTTP(S) proxy, and BOTH aiohttp and
# httpx fail tunneling HTTPS through it (aiohttp: confirmed still-open bug
# aio-libs/aiohttp#8469; httpx: dies mid-TLS-handshake, likely an asyncio
# start_tls-on-an-existing-socket fragility both libraries share). requests
# does the proxy CONNECT + TLS the traditional blocking way, like curl, and
# works fine through this exact proxy. aiohttp is still used everywhere else.
# Floor raised past CVE-2024-47081 (2.32.4) / CVE-2026-25645 (2.33.0).
requests>=2.33.0
aiosqlite==0.22.1
python-dotenv==1.2.2
SQLAlchemy==2.0.47
# Floor raised past the pile of 10.x CVEs (worst: CVE-2023-50447 arbitrary
# code execution; latest fixes CVE-2026-42308/42310 land in 12.2.0).
Pillow>=12.2.0
fonttools>=4.0.0
# Pinned: the API differs across major versions (Beatmap/Performance/Difficulty
# + Strains fields). Pin so prod matches what the strain graph + pp calc are tested on.
rosu-pp-py==4.0.2
# Floor raised past bundled-OpenSSL advisories (GHSA-537c-gmf6-5ccf → 48.0.1).
cryptography>=48.0.1
osrparse>=6.0

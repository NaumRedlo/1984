aiogram==3.29.1
# 3.14.1+: aiohttp 3.13.3 carried 21 published CVEs (fixed across
# 3.13.4/3.14.0/3.14.1). aiogram 3.25 capped aiohttp at <3.14, hence the
# paired aiogram upgrade.
aiohttp==3.14.1
# Beatmap-mirror downloads in utils/osu/beatmap_download.py use requests
# (blocking, run via asyncio.to_thread) instead of aiohttp/httpx: on a host
# whose outbound internet requires an HTTP(S) proxy, BOTH aiohttp and httpx
# fail tunneling HTTPS through it (aiohttp: confirmed still-open bug
# aio-libs/aiohttp#8469; httpx: dies mid-TLS-handshake, likely an asyncio
# start_tls-on-an-existing-socket fragility both libraries share). requests
# does the proxy CONNECT + TLS the traditional blocking way, like curl, and
# works fine through such a proxy. aiohttp is still used everywhere else.
# Floor raised past CVE-2024-47081 (2.32.4) / CVE-2026-25645 (2.33.0).
requests>=2.33.0
aiosqlite==0.22.1
python-dotenv==1.2.2
# The [asyncio] extra, which only pulls in greenlet — and greenlet is what the
# whole async engine runs on, so it is not optional here. SQLAlchemy installs it
# unasked on a list of platform_machine values that includes aarch64 and x86_64
# but not "arm64", which is what macOS on Apple Silicon calls itself: every
# async DB call there dies at setup with "the greenlet library is required".
# The extra asks for it by name and stops depending on which machine reads this.
SQLAlchemy[asyncio]==2.0.47
# Floor raised past the pile of 10.x CVEs (worst: CVE-2023-50447 arbitrary
# code execution; latest fixes CVE-2026-42308/42310 land in 12.2.0).
Pillow>=12.2.0
fonttools>=4.0.0
# Pinned: the API differs across major versions (Beatmap/Performance/Difficulty
# + Strains fields). Pin so prod matches what the strain graph + pp calc are tested on.
rosu-pp-py==4.0.2
# Floor raised past bundled-OpenSSL advisories (GHSA-537c-gmf6-5ccf → 48.0.1).
cryptography>=48.0.1

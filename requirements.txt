aiogram==3.25.0
aiohttp==3.13.3
# Beatmap-mirror/replay downloads in utils/osu/danser_renderer.py use httpx,
# not aiohttp: aiohttp has a confirmed, still-open bug tunneling HTTPS through
# a plain HTTP proxy (aio-libs/aiohttp#8469) that the render worker's outbound
# internet setup requires. aiohttp is still used everywhere else.
httpx>=0.28.0
aiosqlite==0.22.1
python-dotenv==1.2.2
SQLAlchemy==2.0.47
Pillow>=10.0.0
fonttools>=4.0.0
# Pinned: the API differs across major versions (Beatmap/Performance/Difficulty
# + Strains fields). Pin so prod matches what the strain graph + pp calc are tested on.
rosu-pp-py==4.0.2
cryptography>=43.0.0
osrparse>=6.0
trueskill>=0.4.5
libarchive-c>=5.0